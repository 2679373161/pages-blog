{
  "name": "vitepress-project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "npm run generate && vitepress build docs",
    "serve": "vitepress serve docs",
    "generate": "node scripts/generate.js",
    "gen:sidebar": "node scripts/generate-sidebar.js",
    "gen:index": "node scripts/generate-index.js"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.27"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    }
  },
  "dependencies": {
    "@types/node": "^25.2.0",
    "gray-matter": "^4.0.3",
    "mermaid": "^11.12.2"
  }
}
